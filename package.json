{
  "name": "hardhat-project",
  "version": "1.0.0",
  "description": "Smart contracts for CrossMesh AI: AI-powered cross-chain DeFi vault router on CrossFi",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.ts --network crossfiTestnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^6.1.0",
    "@nomicfoundation/hardhat-toolbox-viem": "^4.1.0",
    "@nomicfoundation/hardhat-ethers": "^3.1.0",
    "hardhat": "^2.26.1",
    "@openzeppelin/contracts": "^5.4.0",
    "dotenv": "^17.2.1"
  }
}
